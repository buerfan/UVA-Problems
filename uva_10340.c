#include<stdio.h>
int main()
{
    char s[100],t[100];
    int i,j,length_s,length_t;;
    while(scanf("%s %s",&s,&t)!=EOF)
    {
        length_s=strlen(s);
        length_t=strlen(t);
        for(i=0;i<length_s;i++)
        {
            for(j=0;j<length_t;j++)
            {
                if(s[i])
            }
        }
        printf("%d %d\n",length_s,length_t);
    }
}
